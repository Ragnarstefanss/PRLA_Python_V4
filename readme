Now that you've cloned the repo, you simply need to run testV4.py to set everything up and run the clean script. If you wish to set everything up without running the clean command, run setup.py instead